# Rust build artifacts
*target/
**/*.rs.bk

# Cargo lock (keep for binaries, ignore for libraries — this is a library)
# Cargo.lock  # uncomment if you want to ignore it

# WASM build output (regenerated by wasm-pack)
examples/web/pkg/

# Node / npm
node_modules/
examples/web/node_modules/
examples/web/package-lock.json

# wasm-pack / web example build artifacts
examples/web/target/
examples/web/Cargo.lock

# Local dev examples (not part of the published crate)
# examples/native/

# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*

# Test recordings / snapshots captured during dev
recording.mov
*.mov
*.mp4
*.webm
snapshot.png
*.png

# Android NDK generated bindings (regenerated by build.rs)
rcam-sys-android/src/bindings.rs
